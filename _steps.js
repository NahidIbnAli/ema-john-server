// JWT (Json Web Token)
/*
1. To create a JWT Secret token first write 'node' command on the terminal to write node code
2. Write node modules for generating random number 'require('crypto).randomBytes(10).toString('hex')'
3. Copy the random number that you created by using node modules and set it to .env file
*/
